<h1>All Jobs</h1>

<% @jobs.each do |job| %>
  <ul><%= link_to job.title, job_path(job) %> at <%= job.company.name %></ul>
  <!-- <p><%= link_to "Delete", job_path(job), method: :delete %></p>
  <p><%= link_to "Edit", edit_job_path(job)%></p> -->
<% end %>
